package com.github.redreaperlp.cdpusher.util;

import io.javalin.http.Context;

public class CookieUtil {

    /**
     * Gets the credentials from the cookie (Username, Subname, Lastname, Email, Password) and returns them as a {@link Credentials} object
     * @param ctx The context of the request
     * @return The credentials
     */
    /*public static Credentials getCredentials(Context ctx) {
        return new Credentials(ctx);
    }
    */
}
